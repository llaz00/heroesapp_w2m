export const environment = {
    // APIURL structure = https://[url].[domain]/[apiPath]/service or just [anyWord]
    // If URL structure is changed, please check mock interceptor and ensure service name extraction is still working
    apiUrl: 'mockapi',
    mockApi: true,
};
